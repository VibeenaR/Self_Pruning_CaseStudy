# Self_Pruning_CaseStudy
An implementation of a 3-layer Feed-Forward Network (FFN) that utilizes a learnable Sigmoid-gated mechanism to perform "in-training" weight pruning. This project explores the trade-off between classification accuracy and model sparsity using L1 regularization on dynamically masked weights.
